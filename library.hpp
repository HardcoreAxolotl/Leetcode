#ifndef LIBRARY_HPP
#define LIBRARY_HPP
#include <vector>
#include <string>
// for future refrance
// empty

// library
namespace library {
std::vector<int> twoSum(std::vector<int>& nums, int target);
bool rotateString(std::string s, std::string goal);
}

#endif // LIBRARY_HPP
