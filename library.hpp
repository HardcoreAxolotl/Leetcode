#ifndef LIBRARY_HPP
#define LIBRARY_HPP

// for future refrance
//int c = std::count(list.begin(), list.end(), target); return how many target there is in the array


namespace library {

}

#endif // LIBRARY_HPP
