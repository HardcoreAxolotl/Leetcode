#ifndef LIBRARY_HPP
#define LIBRARY_HPP
#include <vector>
// for future refrance
// empty

// library
namespace library {
std::vector<int> twoSum(std::vector<int>& nums, int target);
}

#endif // LIBRARY_HPP
